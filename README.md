# Phoenix FBI Guard update channel

Public update channel for the MoonLoader script `Phoenix FBI Guard`.

The installed script reads `PhoenixFBIGuard.manifest.txt`, downloads `phoenix_reconnect.lua`, verifies its SHA-256 hash, validates its internal version and Lua syntax, creates a `.bak` backup, and only then replaces the installed file.

Current release: **3.3.1**.